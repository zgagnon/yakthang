{
  "name": "yak-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "features": {},
  "workspaceFolder": "/workspace",
  "remoteUser": "yakshaver",
  "postCreateCommand": "git config --global --add safe.directory '*'",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "golang.go"
      ]
    }
  }
}