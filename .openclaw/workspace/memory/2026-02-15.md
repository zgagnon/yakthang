# 2026-02-15 Session Notes

## Worker Spawning
- Docker workers spawn via `./spawn-worker.sh` in `/home/yakob/yakthang`
- Workers use `OPENCODE_API_KEY` env var (set on host, passed into container)
- OpenCode stores auth at `~/.local/share/opencode/auth.json` when not using env var
- First worker spawn (Yakriel, network-filtering) got stuck — container running but unresponsive, no logs, docker exec/kill hung. Had to force-kill.
- Second spawn (Yakira, net-filter) worked fine — completed in ~4 minutes.

## Network Filtering — DONE
- Task `docker-workers/network-filtering` completed by Yakira
- Created 3 scripts in `scripts/`: setup-worker-network.sh, setup-worker-firewall.sh, update-api-ipset.sh
- Updated `spawn-worker.sh` lines 235-239: tries `yak-workers` network first, falls back to `bridge`
- Uses iptables + ipset approach (Option A from the plan)

## Daily Summary Cron
- Job ID: `a8cc82c5-8b68-4bb0-988d-f067e047245c`
- Schedule: `0 17 * * *` UTC
- Updated prompt to include `./cost-summary.sh --today` for cost data
- Updated delivery to `channel: "slack", to: "yakthang"`
- Slack delivery has `missing_scope` error when using message tool directly
- BUT gateway logs show successful delivery to channel `C0AF4QF10DA` — need to confirm if that's yakthang
- The cron announce delivery may work differently from direct message tool sends

## Slack Config
- Bot is configured with socket mode, botToken + appToken set
- `missing_scope` error on `chat:write` — may need scope added in Slack app settings and reinstall
- Bot has been invited to yakthang channel (user confirmed)
- Old instance was on different host; migrated to baljeet

## Open Tasks
- `cost-tracking` — blocked, ready for implementation
- `validate-migration` — todo
- `git-worktrees` — todo
- `container-hardening` — todo
- `yx feature requests` — 4 items todo

## Host
- Running on `baljeet` (Arch Linux)
- Zellij session name is `didactic-horse` (not `yakthang` — caused shutdown-worker.sh warning)

## Cost Tracking — DONE
- Worker Yakueline (cost-tracker) completed cost-tracking task and all 3 subtasks
- Key fixes: replaced `bc` with `awk` (bc not available), replaced `python3` JSON parsing with `grep`/`awk`
- Fixed `find -mtime -1` → `-mtime 0`, fixed `grep -c` exit codes with `|| true`
- Committed as `9f51a91`: "Fix cost scripts: replace bc/python3 with awk, fix find/grep edge cases"
- Docs changes (8 files, find-and-replace across docs/) left uncommitted — need decision to commit or discard

## Network Filtering Updates
- Added `opencode.ai` to allowed domains in `scripts/update-api-ipset.sh`
- Changed firewall from DROP to ACCEPT (log-only mode) in `scripts/setup-worker-firewall.sh`
- Task `docker-workers/network-filtering` set back to wip (scripts need these changes validated)
- IPv6 concern: `opencode.ai` resolves to Cloudflare IPv6 only — current ipset/iptables is IPv4 only

## Slack Channel
- `#yakthang` channel ID: `C0AF4QF10DA`
- Gateway delivers to Slack successfully via cron announce
- Direct `message` tool sends fail with `missing_scope` error
- Bot can receive messages in yakthang (user confirmed @Yakob works)
